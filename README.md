## Installation

Install the required dependencies by running:

```bash
python3 -m pip install -r requirements.txt
```

```bash
mkdir data/tmp
```

use fillter_location.ipynb to fillter to data in data/raw/flood

use Test_on_HatYai.ipynb
